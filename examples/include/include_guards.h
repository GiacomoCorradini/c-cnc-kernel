#ifndef INCLUDE_GUARDS_H
#define INCLUDE_GUARDS_H

// #pragma once  (alternative)

#include <stdio.h>
#include <stdlib.h>

#define TEST "test"

#define TWICE(o) 2*o

typedef struct {
  int a, b, c, d;
} my_struct_t;

#endif //INCLUDE_GUARDS_H